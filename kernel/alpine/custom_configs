CONFIG_LOCALVERSION="-alpine"
##needed for micon
CONFIG_USB_SERIAL=y
CONFIG_USB_SERIAL_CONSOLE=y
CONFIG_USB_SERIAL_GENERIC=y
CONFIG_USB_SERIAL_PL2303=y

##build in the oot driver
CONFIG_USB_RTL8152=y

####overdue to try this again? kernel should work userspace is another matter
#CONFIG_THUMB2_KERNEL=y

#CONFIG_ARM_PAGE_SIZE_32KB=y
#CONFIG_ARM_PAGE_SIZE_LARGE_SHIFT=15

CONFIG_ARM_PAGE_SIZE_32KB=n
CONFIG_ARM_PAGE_SIZE_LARGE=y
# CONFIG_ARM_PAGE_SIZE_64KB is not set
# CONFIG_ARM_PAGE_SIZE_32KB is not set

CONFIG_ARM_PAGE_SIZE_4KB=y
CONFIG_ARM_PAGE_SIZE_LARGE_SHIFT=12

#CONFIG_ARM_PAGE_SIZE_64KB=y
#CONFIG_ARM_PAGE_SIZE_LARGE_SHIFT=16

##some not fully vetted performance tweaks
CONFIG_PREEMPT_NONE_BUILD=y
CONFIG_PREEMPT_NONE=y
CONFIG_PREEMPT=n
CONFIG_TINY_RCU=y
CONFIG_TINY_SRCU=y
CONFIG_INLINE_SPIN_UNLOCK_IRQ=y
CONFIG_INLINE_READ_UNLOCK=y
CONFIG_INLINE_READ_UNLOCK_IRQ=y
CONFIG_INLINE_WRITE_UNLOCK=y
CONFIG_INLINE_WRITE_UNLOCK_IRQ=y

CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y

##might improve performance to remove from kernel if cpu doesn't support anyway
CONFIG_CPU_FREQ=n

##needed for devices with > 2GB memory
CONFIG_HIGHMEM=y

## smaller kernel, why not?
CONFIG_KERNEL_XZ=y
